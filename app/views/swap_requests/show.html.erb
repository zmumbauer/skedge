<div class="page-container">
  <div class="padded-container">
    <div class="body">Submitted by: <%= User.find(@swap_request.requesting_user_id).full_name %></div>
    <div class="body"><%= Timeperiod.find(@swap_request.timeperiod_id).start_time.strftime("%b %e") %></div>
    <div class="body"><%= Timeperiod.find(@swap_request.timeperiod_id).start_time.strftime("%l:%M%P") %> — <%= Timeperiod.find(@swap_request.timeperiod_id).end_time.strftime("%l:%M%P") %></div>
  </div>
</div>
